# Pinned to versions known to work on Python 3.13 and 3.14.
# If you're on Python 3.12 or older, the same versions still install cleanly.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pymupdf>=1.24
pytesseract>=0.3.10
pillow>=11
pydantic>=2.10
python-multipart>=0.0.20
